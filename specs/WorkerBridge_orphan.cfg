\* Checks only the "every promise settles" property, so TLC reports the
\* orphaned-export counterexample rather than stopping at the cross-talk one.
SPECIFICATION Spec

CONSTANT MaxReq = 3

INVARIANT TypeOK
INVARIANT NoOrphan
